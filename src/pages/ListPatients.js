import React from 'react';
import GlobalStyle from '../styles/globalStyle';
import { Title, Header, Footer } from '../styles/styles';

const ListPatients = () => {
  return <>
    <GlobalStyle />
    <Header>logo</Header>
    <Title>Ver/editar pacientes</Title>
    <Footer></Footer>
  </>;
};

export default ListPatients;
